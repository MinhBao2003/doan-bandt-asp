@model IEnumerable<DoAn_Nhom3.Models.BinhLuan>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NoiDungbl)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NoiDungbl)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Tenbl }) |
            @Html.ActionLink("Details", "Details", new { id=item.Tenbl }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Tenbl })
        </td>
    </tr>
}

</table>
